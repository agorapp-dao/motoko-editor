To define an immutable variable use the `let` keyword:

```motoko
let x = 10;
```

Immutable variable cannot change its value after it's been assigned.

## Exercise

Code on the right does not compile. Fix it.
